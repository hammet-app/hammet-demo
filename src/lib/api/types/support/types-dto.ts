// ============================================================
// SUPPORT ROUTES
// ============================================================

// POST /callback
export type CallbackFormDto = {
  school_name: string;
  full_name: string;
  email: string;
  role: string;
  phone: string;
  city: string;
}